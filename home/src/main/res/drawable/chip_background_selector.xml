<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Выбранный чип -->
    <item android:color="@color/selected_chip_background_color" android:state_checked="true"/>
    <item android:color="@color/selected_chip_background_color" android:state_selected="true"/>

    <!-- Не выбранный чип -->
    <item android:color="@android:color/black"/>

</selector>